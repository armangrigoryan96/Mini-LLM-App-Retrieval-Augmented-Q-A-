# API Keys
OPENAI_API_KEY=your_openai_api_key_here

# Model Configuration
EMBEDDING_MODEL=text-embedding-3-small
LLM_MODEL=gpt-4o
EMBEDDING_DIMENSIONS=1536

# RAG Settings
CHUNK_SIZE=1000
CHUNK_OVERLAP=200
TOP_K=5

# Scraper Configuration (for testing with limited data)
# Set MAX_DOCS to limit number of documents fetched (None or 0 for all 35 docs)
# Set MAX_CHUNKS_PER_DOC to limit chunks per document (None or 0 for all chunks)
# NOTE: For full QA dataset coverage (20 questions), use MAX_DOCS=0 or remove these limits
# Current default (MAX_DOCS=2) is for cost-controlled testing only
MAX_DOCS=0
MAX_CHUNKS_PER_DOC=0

# Milvus Connection (optional, defaults to localhost:19530)
# MILVUS_HOST=localhost
# MILVUS_PORT=19530
